SELECT * FROM `Datenbankname` 		Suche in einem Datenkey

WHERE `Datenkey` = `Suche…`

SELECT * FROM `Datenbankname` WHERE `Datenkey` LIKE `Suche%`			bei Like wird nach einem Datensatz gesucht der so beginnt

DROP table `Datenbankname`		löschen einer Datenbank

WHERE					Abfrage ob etwas enthält

LIKE						Abfrage ob etwas beginnt mit

UPDATE ….SET				Updatet und setzt neuen Wert

INSERT INTO () VALUES (‘‘,‘‘)			Fügt einen neuen Datensatz mit den nach Values definierten Daten hinzu

DELETE FROM…				löscht einen Datensatz
